# macOS finder cruft
.DS_Store
# Custom-icon resource-fork marker (Finder's "paste image as icon")
Icon
Icon?

# Python bytecode
__pycache__/
*.pyc

# Server logs / temp state
/tmp/memorymap.pid

# Per-machine config (workspace path, external data file override)
data/config.local.json
